{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "noEmit": true,
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "jsx": "react-jsx",
    // `vitest` 必须显式映射（issue #68）：本包 node_modules 下是 vitest 3.2.7，而仓库根
    // node_modules 下是 vitest 2.1.9（根 package.json 给 scripts/ 守卫测试用的）。
    // `@testing-library/jest-dom/vitest` 的 `declare module 'vitest'` 是**从它自己的位置**
    // 解析 `'vitest'` 的：jest-dom 在 pnpm 的 .pnpm 树下没有自己的 vitest ⇒ 向上走到
    // **根** node_modules ⇒ 它把 `toBeInTheDocument` 增强合并进了 2.1.9 的 `Assertion`，
    // 而测试文件 import 的是本包的 3.2.7 ⇒ 两个 `Assertion` 不是同一个声明 ⇒
    // 「Property 'toBeInTheDocument' does not exist」× 40。显式 paths 把两侧钉到同一个
    // 文件（apps/web/tsconfig.app.json 早已这么写，此处对齐）。
    //
    // ⚠️ 本地写 `paths` 会**整体覆盖** tsconfig.base 的 paths（extends 不合并该字段）——
    // 这里故意只留 `vitest`：`@platform/sdk` / `@platform/auth-core` 走 node_modules 的
    // workspace 软链 + package.json `exports` 同样解析得到（本模块未用 auth-core），
    // 复制一份反而多一处要跟着 base 改的漂移点。
    "paths": {
      "vitest": ["./node_modules/vitest/dist/index.d.ts"]
    }
  },
  // 测试文件**纳入** typecheck（issue #68，2026-09-17 收口）：旧写法 `["**/*.test.ts",
  // "**/*.test.tsx", "mobile"]` 里的前两条已删。注意 .tsx 也回到检查面内——M3a 起本模块
  // 有 console 组件测试，正是需要的。
  // 不写 `include`（原为 `["**/*.ts"]`）：那会把 console 的 .tsx 排除在类型检查外，
  // 与 modules/demo 的形状对齐（demo 也是无 include、只写 exclude）。
  //
  // ⚠️ `mobile` 是**嵌套的独立 workspace 包**（M3b-2），必须**保持**排掉（2026-09-16 Wave 4
  // 实测，issue #68 Step 3 复核）：本配置无 `include` ⇒ 父 tsc 会**把 mobile/src/** 一并纳入**
  // （实测 18 个文件，含 `env.d.ts` / `vite.config.ts`），而它用的是**本包的**编译选项——
  // 不知道 mobile 自己的 `paths` 别名 ⇒ `@wujibase/*` / `@/*` 全报 TS2307（实测 23 条假红），
  // 把整个 `pnpm typecheck` 打红。**这一条不要随「清 exclude」一起删**。
  // 排掉**不是漏检**：mobile 有**自己的** `typecheck`（`vue-tsc --noEmit` + 它自己的 tsconfig），
  // `pnpm -r --if-present typecheck` 会跑到它 ⇒ 该包仍被检查，只是用**对的配置**检查一次。
  "exclude": ["mobile"]
}
