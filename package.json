{
  "name": "platform-core",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "packageManager": "pnpm@11.22.0",
  "scripts": {
    "test": "pnpm -r --if-present test && pnpm run test:guard",
    "test:guard": "vitest run --dir scripts",
    "build": "pnpm -r --if-present build",
    "dev": "pnpm --filter @platform/server dev"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.9",
    "yaml": "^2.9.0"
  }
}
